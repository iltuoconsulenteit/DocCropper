version: '3.9'
services:
  doccropper:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8765:8765"
    volumes:
      - ../env:/opt/doccropper/env
      - ../users:/opt/doccropper/users
